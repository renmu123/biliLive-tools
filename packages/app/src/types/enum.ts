export enum TaskType {
  danmu = "danmu",
  ffmpeg = "ffmpeg",
  bili = "bili",
  biliDownload = "biliDownload",
}

export enum NotificationType {
  server = "server",
  mail = "mail",
  tg = "tg",
}
