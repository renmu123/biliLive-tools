# AI 协作规范

当 AI 代表用户发布 Issue 或 Pull Request 时，必须在 Issue 或 Pull Request 的正文中明确添加“由AI发布”。

# 基本命令

```bash
pnpm run format   # 格式化代码
pnpm run test     # 运行测试
```

# 依赖

`node`版本参照`.node-version`，`pnpm`版本参考`package.json/packageManager`字段
