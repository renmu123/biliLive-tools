{
  "name": "@biliLive-tools/root",
  "version": "1.0.0-alpha.1",
  "description": "B站直播录制处理工具",
  "author": "renmu123",
  "license": "GPL-3.0-only",
  "homepage": "https://github.com/renmu123/biliLive-tools",
  "type": "module",
  "scripts": {
    "build:app": "pnpm run --filter @biliLive-tools/shared build  && pnpm run --filter @biliLive-tools/http build && pnpm run --filter biliLive-tools build:app",
    "build:app:no-ffmpeg": "pnpm run --filter @biliLive-tools/shared build && pnpm run --filter @biliLive-tools/http build && pnpm run --filter biliLive-tools build:app:no-ffmpeg",
    "preinstall": "npx only-allow pnpm",
    "install:bin": "node ./scripts/install-bin.js"
  },
  "dependencies": {
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "cli-progress": "^3.12.0",
    "download": "^8.0.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5"
  }
}
